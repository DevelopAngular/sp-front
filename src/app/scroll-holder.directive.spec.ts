import { ScrollHolderDirective } from './scroll-holder.directive';

describe('ScrollHolderDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHolderDirective();
    expect(directive).toBeTruthy();
  });
});
