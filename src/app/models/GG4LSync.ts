export class GG4LSync {
  last_failed_sync: string;
  last_successful_sync: string;
  next_sync: string;
  num_accounts: number;
  num_students: number;
  num_teachers: number;
  num_assistants: number;
  num_admins: number;
  is_enabled: boolean;
  is_syncing: boolean;
  is_configured: boolean;
}
