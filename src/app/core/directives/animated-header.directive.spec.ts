import { AnimatedHeaderDirective } from './animated-header.directive';

describe('AnimatedHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
