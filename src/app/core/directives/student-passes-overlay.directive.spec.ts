import {StudentPassesOverlayDirective} from './student-passes-overlay.directive';

describe('StudentPassesOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentPassesOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
