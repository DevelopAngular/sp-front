import { DomElementCheckerDirective } from './dom-element-checker.directive';

describe('DomElementCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new DomElementCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
