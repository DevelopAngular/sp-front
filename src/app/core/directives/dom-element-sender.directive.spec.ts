import { NavbarElementSenderDirective } from './navbar-element-sender.directive';

describe('NavbarElementSenderDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarElementSenderDirective();
    expect(directive).toBeTruthy();
  });
});
