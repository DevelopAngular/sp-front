import {SwipeEventDirective} from './swipe-event.directive';

describe('SwipeEventDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeEventDirective();
    expect(directive).toBeTruthy();
  });
});
