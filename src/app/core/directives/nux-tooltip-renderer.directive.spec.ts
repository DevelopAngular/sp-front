import {NuxTooltipRendererDirective} from './nux-tooltip-renderer.directive';

describe('NuxTooltipRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new NuxTooltipRendererDirective();
    expect(directive).toBeTruthy();
  });
});
