import { PassClickDirective } from './pass-click.directive';

describe('PassClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PassClickDirective();
    expect(directive).toBeTruthy();
  });
});
